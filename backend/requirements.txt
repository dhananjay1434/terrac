fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.22.1
pydantic==2.10.5
python-multipart==0.0.20
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.2
psycopg2-binary==2.9.10
alembic==1.12.1
piexif==1.1.3
# Ed25519 device-signature verification (server.py) — a hard runtime dependency,
# previously satisfied only by the ambient conda env (P0.2).
cryptography==44.0.3
# .env loading in server.py + alembic/env.py (P0.2).
python-dotenv==1.0.1
# Portal password hashing (P2.1) — argon2id via argon2-cffi.
argon2-cffi==25.1.0
# Evidence media object storage (P3.2) — S3/MinIO/GCS-interop backend via boto3.
# The default LocalMediaStorage path does not import boto3, so it stays optional
# at runtime, but it is pinned so the s3 backend + MinIO CI job are reproducible.
boto3==1.35.99
# Observability (P3.4). prometheus_client backs the guarded /metrics endpoint;
# sentry-sdk is the error reporter (no-op unless DMRV_SENTRY_DSN is set). Both
# are imported defensively in observability.py so their absence never bricks boot.
prometheus-client==0.21.1
sentry-sdk[fastapi]==2.20.0
