# ui/

Primitive, reusable, presentational components (Button, Card, StatusPill, …).
No business logic, no data fetching, no page-specific assumptions. Every
component is colocated with its own `.module.css` and `.test.tsx`, uses design
tokens only (no raw hex/px), and is keyboard-accessible.
