# tokens/

Design tokens: the single source of truth for color, space, type, and radius.
CSS custom properties live in `tokens.css`; the typed TS mirror lives in `tokens.ts`.
Nothing in this folder renders UI or holds business logic.
