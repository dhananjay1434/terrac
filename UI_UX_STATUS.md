# UI/UX Migration Status

Tracking execution of `REMEDIATION_PROMPT_UI_UX.md`.

```
 UX-A1 [x] ChartFrame primitive (NEW files, verbatim) + test        (additive; 0 breaks)
 UX-B1 [x] Migrate ThermalMapChart onto ChartFrame (worked example)
 UX-B2 [x] Migrate LoadTelemetryChart onto ChartFrame
 UX-B3 [x] Migrate TemperatureChart onto ChartFrame
 UX-B4 [x] Migrate BarChart onto ChartFrame
 UX-B5 [-] HorizontalBarList/BurnTelemetryChart/DivergingStackedBarChart — SKIPPED, see log
 UX-C1 [x] CellStack primitive (NEW) + test
 UX-C2 [x] Migrate Ledger / UnboundSessions / JourneyPanel tables → DataTable + CellStack
 UX-D1 [x] Extract page data into features/*/useX hooks (Dispatch, BatchDetail, Projects)
 UX-E1 [x] DetailDrawer (promote JourneyPanel shell) + MetricBlock rows
 UX-F1 [x] Polish layer: motion + focus-visible + count-up (per §4 of the blueprint) — mostly pre-existing, see log
 UX-F2 [x] State layer: 4-state taxonomy (loading/empty/filtered-empty/error) + CLS-safe skeletons
 UX-F3 [x] Responsive: DataTable mobile (card/scroll), ChartFrame narrow, DetailDrawer full-width
 UX-G1 [x] Guard tests: no raw <table> in pages, no inline chart <svg>, no fetch in page JSX
 UX-G2 [x] A11y: fix z-index order (tooltip topmost); chart aria-describedby; icon-btn aria-label guard
 FINAL [x] tsc + vitest green (minus the 1 known); Visual QA (§0.8) NOT run — no browser available this session, see log
```

## Log
- **UX-F1/F2/F3/G1/G2** (2026-08-01): Audited before writing new code — most of §4's polish layer was ALREADY in place pre-migration: global `:focus-visible` ring (`styles.css`), token-driven row/button hover transitions (`--dur-micro` via the `--duration-fast` bridge alias), and `prefers-reduced-motion` wrapping (`BurnLiveView`, and now `DetailDrawer`'s slide-in). Did NOT add: a blanket Card hover-lift (Card isn't always an interactive/clickable surface — adding a hover affordance to a non-clickable card would misrepresent it) or a `StatTile` count-up (its sibling `MetricBlock` already has a deliberate, documented "no count-up — static numbers read as more trustworthy for financial figures" decision in this codebase; extending that logic, and because many `StatTile` values are non-numeric strings like "active"/"—", a generic counter would need per-call-site numeric parsing and would risk breaking the many tests that assert `StatTile` text synchronously right after render).
  - **F2**: grepped for bare "Loading…" strings outside the existing `Skeleton`/`DataTable` skeleton-row/`EmptyState`/`CardError` machinery — found exactly one gap, `Ledger.tsx`'s `<div>Loading ledger...</div>`, fixed to 4 dimension-matched `Skeleton` cards (CLS-safe, matches the loaded grid's shape).
  - **F3**: `DataTable` now wraps its `<table>` in its own `overflow-x:auto` container (first column sticky under 768px) — a single primitive fix that covers every table call site, so no page-by-page work was needed. `ChartFrame` now drops to 3 y-ticks below a 400px measured width instead of 5 (avoids label collision); its legend was already below the plot, not beside it. `DetailDrawer` was already full-width under 768px from UX-E1.
  - **G1**: `src/tokens/architecture.test.ts` — no raw `<table>` / hand-drawn chart marks (`<polyline>`, filled `<rect>`) in `src/pages/*.tsx` (both already clean); and the 3 de-godded pages (`Dispatch`/`BatchDetail`/`Projects`) import no data-fetch function directly from `api`/`api2` — they must go through their `useX` hook. Scoped to those 3 rather than all pages, since blanket-enforcing "no fetch in any page" would immediately fail every page NOT in UX-D1's stated scope (e.g. `Batches.tsx`, `Farmers.tsx` still fetch directly — that's expected, unchanged behavior, not a regression).
  - **G2**: fixed the z-index scale in `tokens.css` (`--z-tooltip` 30→110, now above `--z-drawer`(60)/`--z-modal`(100); `--z-skiplink`(200) stays highest) with a new ordering guard test in `tokens.test.ts`. Added an optional `summary` prop to `ChartFrame` (visually-hidden text wired via `aria-describedby`) and wired it into `ThermalMapChart` (channels + peak) and `LoadTelemetryChart` (latest + peak) — this duplicated visible badge text into an accessible-name query in one existing test (`BurnLiveView.test.tsx`), corrected to scope the query to the visible `[data-gate]` badge specifically (the assertion's intent — "the badge shows the new max" — is unchanged). Added `src/tokens/a11y-guards.test.ts`: every `<button>` whose only content is an `aria-hidden` icon must carry `aria-label`/`aria-labelledby` — audited the whole codebase by hand first (`CopyButton`, `InfoTip`, `MediaCell`, `AppShell/Sidebar`, `AppShell/Topbar`, `DetailDrawer` were all already correctly labeled) and confirmed the guard passes with zero offenders before relying on it.
  - **FINAL GATE**: `tsc --noEmit` clean; `vitest run` 429/430, only the known `AppShell` snapshot red (re-ran 3× to rule out flake — one run separately hit an unrelated transient axe color-contrast timeout, gone on retry). Visual QA (§0.8 — light/dark × 1440/1024/390px in a live browser) was **not performed**: this session has no browser available. Everything claimed above is verified by source/DOM-level tests, not by eyes on a rendered page — say so explicitly rather than claiming a visual pass that didn't happen.
- **UX-E1** (2026-08-01): Created `src/ui/DetailDrawer/{DetailDrawer.tsx,DetailDrawer.module.css,DetailDrawer.test.tsx}` — a right-side slide-in panel built on Radix `Dialog` (so focus-trap, ESC-close, scrim-click-close, and focus-return-to-trigger are inherited for free, not reimplemented), full-width sheet under 768px, slide-in animation wrapped in `prefers-reduced-motion`. Routed `Dispatch.tsx`'s inline `Card`-based journey-detail panel through it (dropped the now-redundant `detailRef`/`scrollIntoView` plumbing from `useDispatch.ts` — a fixed overlay drawer doesn't need to be scrolled into view). Did NOT force `JourneyPanel`'s existing `<dl>`/`dt`/`dd` metric rows (Distance/Vehicle/Fuel/Est. emissions) onto `MetricBlock` — `MetricBlock` is sized for one hero stat (already used that way in `BatchDetail`'s net-credit callout), not a compact multi-row list, and JourneyPanel's rows are well-tested (8 passing tests) and already serve that layout correctly. 4/4 new tests green, GATE E green (421/422, only the known `AppShell` snapshot red).
- **UX-D1** (2026-08-01): Extracted all state/fetch/handler logic out of the 3 god pages into `src/features/*/useX.ts` hooks; the pages now compose the hook's returned state into JSX only. `Dispatch.tsx` 436→246 lines (`useDispatch.ts` 236 — list pagination + facility form + journey detail). `BatchDetail.tsx` 401→301 lines (`useBatchDetail.ts` 156 — batch/timeline/capability fetch + issue/export actions + derived chain nodes); kept `STEP_ORDER`/`STEP_TITLES`/`groupMedia` exported from the page file unchanged since `EvidenceGallery.tsx` and 2 test files import them directly. `Projects.tsx` 392→165 lines (`useProjects.ts` 278 — project list/form + parcel list/form, both under the 150-line target). All 3 pages stayed above the literal "<150 lines" target except Projects — the remainder in Dispatch/BatchDetail is JSX composition (forms, hero layout, conditional panels), not logic; further splitting would fragment presentation without reducing real complexity. GATE D green (417/418, only the known `AppShell` snapshot red).
- **UX-A1** (2026-08-01): Created `src/ui/ChartFrame/{ChartFrame.tsx,ChartFrame.module.css,ChartFrame.test.tsx}` verbatim. `tsc --noEmit` clean, 4/4 tests green.
- **UX-B1** (2026-08-01): ThermalMapChart now composes ChartFrame (title/legend/badge via props); manual head div + end-labels removed. 5/5 tests green, no correction needed.
- **UX-B2** (2026-08-01): LoadTelemetryChart composes ChartFrame (chips → `headerStats`), added an emphasized endpoint marker per §UX-B* acceptance criteria. Corrected the circle-count assertion 4→5 (endpoint marker is new, intentional).
- **UX-B3** (2026-08-01): TemperatureChart composes ChartFrame for both the single-reading and multi-reading paths; deduped `yTicks` to avoid a React key collision when all readings are equal. Corrected 2 tests whose assertions were about the OLD chrome's inline mechanics (exact plot-bound pixels 192→180 now that ChartFrame owns `X_AXIS_H`; fill-attribute styling → shared `.axisLabel`/`.micro` class) — structural/count assertions kept, only the source-of-truth values changed.
- **UX-B4** (2026-08-01): BarChart composes ChartFrame with `yTicks={[]}` (BarChart never had numeric y-axis labels, only decorative gridlines with no test coverage) so category labels stay under the 8-label thinning cap. All 11 existing tests green with zero corrections.
- **UX-C1** (2026-08-01): Created `src/ui/CellStack/{CellStack.tsx,CellStack.module.css,CellStack.test.tsx}` verbatim. 3/3 tests green.
- **UX-C2** (2026-08-01): Migrated the 3 raw `<table>`s to `DataTable`+`ColumnDef`: `Ledger.tsx` (period/batches/biomass/species-pills/batch-code-links), `UnboundSessions.tsx` (session/device/started/chunks/bind-button, no test regressions), `JourneyPanel.tsx` (manifest container/count/mass-or-volume/product). None of the three had a natural primary+secondary pairing that called for `CellStack` (every column is single-line), so `CellStack` is created but not yet consumed — left for Phase E's `DetailDrawer`/`MetricBlock` work or a future table with a name+location-shaped column. GATE C green (417/418, only the known `AppShell` snapshot red).
- **UX-B5** (2026-08-01): **Skipped, by design — not a ChartFrame fit.** `HorizontalBarList` is a CSS/HTML bar list, no SVG at all. `BurnTelemetryChart` is a pure data-source switcher (v2 live vs. legacy `TemperatureChart`) with no chrome of its own — nothing to migrate. `DivergingStackedBarChart` has a diverging, per-label zero-baseline scale (not a single linear yDomain) plus an interactive floating tooltip and focusable per-bar hit-areas requiring `role="group"` — ChartFrame hardcodes `role="img"`, and nesting focusable controls inside an img-roled region is an ARIA violation the component's own code comments explicitly warn against. Forcing these into ChartFrame would misrepresent the abstraction rather than remove real duplication; the 4 charts in B1–B4 were the actual shared-chrome duplication target.
